<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>databae</title>
  <link rel="stylesheet" type="text/css" href="/stylesheet/style.css">
</head>

<body>
  <div class="wrapper">
    <header>
      <h1 class='logo'>
        <a href="/">{databae}</a>
      </h1>
      <nav>
        {{#if loggedIn}}
        <button class="blog-nav" href="/">home</button>
        <button class="dashboard-nav" href="/dashboard">dashboard</button>
        <button class="dashboard-nav" href="/chat">chat</button>
        <button id="logout" class="btn-no-style">logout</button>
        {{else}}
          <button class="login-heading" href="/login">login</button>
        {{/if}}
      </nav>
    </header>
    <main>
      {{{ body }}}
    </main>
  </div>
  
  {{#if loggedIn}}
  <script src="js/logout.js"></script>
  {{/if}}
</body>

</html>